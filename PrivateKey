!pip install cryptography
from cryptography.fernet import Fernet
key = Fernet.generate_key()
f = Fernet(key)
token = f.encrypt(b"Blockchain implementation in python ")
print(token)
d = f.decrypt(token)
print(d.decode())
